<!DOCTYPE html>
<html>
 <head>
   <title>Catpower Lille</title>
   <%= csrf_meta_tags %>
   <%= csp_meta_tag %>

   <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
   <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>


<link href="https://fonts.googleapis.com/css?family=Nunito:400,600,700,800|Roboto:400,500,700" rel="stylesheet">
<link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.0.13/css/all.css" integrity="sha384-DNOHZ68U8hZfKXOrtjWvjxusGo9WQnrNx2sqG0tfsghAvtVlRW3tvkXWZh58N9jp" crossorigin="anonymous">


 </head>

  <body>

  
<div class="container">
  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>
</div>




    <!-- MAIN WRAPPER -->
    <div class="body-wrap shop-default shop-minimalist">
      <div id="st-container" class="st-container">
        <div class="st-pusher">
          <div class="st-content">
            <div class="st-content-inner">

              <!-- Header -->
              <div class="header">
                <!-- Top Bar -->

                <!-- Navbar -->
                <nav class="navbar navbar-expand-lg navbar--bold navbar--style-2 navbar-light bg-default  navbar--bb-1px">
                  <div class="container navbar-container">
                    <!-- Brand/Logo -->
                    <a class="navbar-brand" href="<%= root_path %>">
                      <img src="<%= image_path'logo/logo-2-b.png' %>" class="" alt="Boomerang">
                    </a>

                    <div class="d-inline-block">
                      <!-- Navbar toggler  -->
                      <button class="navbar-toggler hamburger hamburger-js hamburger--spring" type="button" data-toggle="collapse" data-target="#navbar_main" aria-controls="navbarsExampleDefault" aria-expanded="false" aria-label="Toggle navigation">
                        <span class="hamburger-box">
                          <span class="hamburger-inner"></span>
                        </span>
                      </button>
                    </div>

                    <div class="collapse navbar-collapse" id="navbar_main">
                      <!-- Navbar search - For small resolutions -->
                      <div class="navbar-search-widget b-xs-bottom py-3 d-lg-none d-none">
                        <form class="" role="form">
                          <div class="input-group input-group-lg">
                            <input type="text" class="form-control" placeholder="Search for...">
                            <span class="input-group-btn">
                              <button class="btn btn-base-3" type="button">Go!</button>
                            </span>
                          </div>
                        </form>
                      </div>

                      <!-- Navbar links -->
                      <ul class="navbar-nav">
                        <li class="nav-item dropdown megamenu">
                           <%= link_to "Les produits", shop_path, class: 'nav-link' %>
                        </li>
                        <li class="nav-item dropdown megamenu">
                          <%= link_to "Contact", home_contact_path, class: 'nav-link' %>
                        </li>
                        <% if user_signed_in? %>
                        <li class="nav-item dropdown megamenu">
                          <%= link_to "Panier", panier_path, class: 'nav-link' %>
                        </li>
                        <li class="nav-item dropdown megamenu">
                          <%= link_to "Deconnexion", destroy_user_session_path, method: :delete, class: 'nav-link' %>
                        </li>
                        <% else %>
                        <li class="nav-item dropdown megamenu">
                          <%= link_to "Connexion", new_user_session_path, class: 'nav-link' %>
                        </li>
                        <li class="nav-item dropdown megamenu">
                          <%= link_to "Inscription", new_user_registration_path, class: 'nav-link' %>
                        </li>
                        <% end %>
                      </ul>
                    </div>

                    <div class=" d-none d-lg-inline-block d-xl-inline-block">
                    <ul class="navbar-nav ml-auto" data-hover="dropdown">
                      <!-- Login -->
                      <li class="nav-item nav-item-icon">
                        <a href="#" class="nav-link hidden-md-down" data-toggle="dropdown">
                          <i class="fa fa-user"></i>
                        </a>
                        <ul class="dropdown-menu dropdown-menu--right dropdown-form">
                          <li>
                            <div class="dropdown-form-inner">
                              <div class="row align-items-center b-xs-bottom">
                                <div class="col-12">
                                  <% if user_signed_in? %>
                                  <h3 class="heading heading-5 strong-400 pb-2"><%= current_user.fullname %></h3>
                                  <h3 class="heading heading-5 strong-400 pb-2"><%= current_user.email %></h3>
                                  <% else %>
                                  <h3 class="heading heading-5 strong-400 text-capitalize pb-2">connexion</h3>
                                  <% end %>
                                </div>
                              </div>
                               <div class="py-4">
                                <% if user_signed_in? %>
                                <%= link_to "Mon profil", users_show_path, class:"btn btn-styled  btn-base-1" %>
                                <% end %>
                                </div>
                              <div class="py-4">
                                <% if user_signed_in? %>
                                <%= link_to "Deconnexion", destroy_user_session_path, method: :delete, class:"btn btn-styled  btn-base-2" %>
                                <% else %>
                                <%= form_for(resource, as: resource_name, url: session_path(resource_name), class:"form-default") do |f| %>
                                  <div class="form-group">
                                    <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'form-control form-control-lg', placeholder: 'Email' %>
                                  </div>

                                  <div class="form-group">
                                    <%= f.password_field :password, autocomplete: "off", class: 'form-control form-control-lg', placeholder: 'Mot de passe' %>
                                  </div>

                                  <div class="form-group mb-0">
                                    <div class=" row align-items-center">
                                      <div class="col-6">
                                        <% if devise_mapping.rememberable? -%>
                                        <div class="checkbox ml-2">
                                          <%= f.check_box :remember_me %>
                                          <%= f.label :remember_me, 'Se rappeler de moi', class:'no-margin' %>
                                        </div>
                                        <% end %>
                                      </div>

                                      <div class="col-6 text-right">
                                        <%= f.submit "Connexion", class:"btn btn-base-1 btn-circle" %>

                                      </div>
                                    </div>
                                  </div>
                                <% end %>
                                <% end %>
                              </div>
                            </div>
                          </li>
                          <% if user_signed_in? %>
                          <% else %>
                          <li class="text-center py-3 b-xs-top">
                            <%= link_to "Inscription", new_user_registration_path %>
                          </li>
                          <% end %>
                        </ul>
                      </li>
                      
                      <!-- Cart -->
                      <% if user_signed_in? %>
                      <li class="nav-item nav-item-icon">
                        <a class="nav-link" href="<%= panier_path %>">
                          <i class="fa fa-shopping-bag"></i>
                        </a>
                      </li>
                      <% end %>
                    </ul>
                  </div>
                  
                </div>
              </nav>
              <!-- END Navbar -->
          </div>

          
          


         
    <%= yield %>
    <!-- FOOTER -->
    <footer id="footer" class="footer">
      <div class="footer-top">
        <div class="container">
          <div class="row cols-xs-space cols-sm-space cols-md-space">
            <div class="col-lg-5">
              <div class="col">
                <img src=<%= image_path 'logo/logo-2-b.png' %>>
                <span class="clearfix"></span>
                <span class="heading heading-sm c-gray-light strong-400">Un animal. Des milliers d'affiches.</span>
                <p class="mt-3">
                  Tous les chats présentés dans notre magasin ont donné leur consentement. Aucune maltraitance envers les félins bien-sûr ! Les chats vaincront.
                </p>

                <div class="copyright mt-4">
                  <p>
                    Copyright &copy; 2018                                <a href="https://www.thehackingproject.org/" target="_blank">
                      <strong>THP - Lille</strong>
                    </a> -
                    All rights reserved
                  </p>
                </div>
              </div>
            </div>
      </div>
    </div>
  </div>
</footer>
      <!-- END FOOTER -->

    </div>
  </div>
</div><!-- END: st-pusher -->
</div>
</div><!-- END: body-wrap -->
  </body>
</html>
